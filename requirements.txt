# Data processing
polars
pandas
pyarrow>=17.0.0

# Lakehouse / Iceberg / MinIO
# pyiceberg-core: Rust extension required to apply partition transforms
# (e.g. month(time)) when writing to partitioned Iceberg tables.
pyiceberg[s3fs,pyiceberg-core]
boto3==1.43.0
botocore==1.43.0
s3fs==2026.4.0
aiobotocore==3.7.0
fsspec==2026.4.0

# Data quality: validate bằng Polars (xem data_quality.md) — không cần thư viện ngoài

# Data sources
vnstock
holidays
requests
plotly
kafka-python

# Serving database
clickhouse-connect
streamlit

# Configuration and reliability
python-dotenv
tenacity
pydantic

# Development / testing
pytest
ipykernel
